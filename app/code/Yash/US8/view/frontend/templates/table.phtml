<?php
$sortField = $this->getRequest()->getParam('sort', 'employee_id');
$sortDir = $this->getRequest()->getParam('dir', 'ASC');
?>
<table border="1">
    <thead>
        <tr>
            <th><a href="?sort=employee_id&dir=<?= $sortDir == 'ASC' ? 'DESC' : 'ASC' ?>">Employee ID</a></th>
            <th><a href="?sort=first_name&dir=<?= $sortDir == 'ASC' ? 'DESC' : 'ASC' ?>">First Name</a></th>
            <th><a href="?sort=last_name&dir=<?= $sortDir == 'ASC' ? 'DESC' : 'ASC' ?>">Last Name</a></th>
            <th>Email</th>
            <th>Address</th>
            <th>Phone</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($block->getEmployees($sortField, $sortDir) as $employee): ?>
            <tr>
                <td><?= $employee->getEmployeeId() ?></td>
                <td><?= $employee->getFirstName() ?></td>
                <td><?= $employee->getLastName() ?></td>
                <td><?= $employee->getEmailId() ?></td>
                <td><?= $employee->getAddress() ?></td>
                <td><?= $employee->getPhoneNumber() ?></td>
                <td>
                    <form method="post" action="<?= $block->getUrl('employee/index/delete') ?>">
                        <input type="hidden" name="employee_id" value="<?= $employee->getId() ?>">
                        <button type="submit">Delete</button>
                    </form>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>